apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "coder-bootstrap.fullname" . }}-script
  labels:
    {{- include "coder-bootstrap.labels" . | nindent 4 }}
data:
  bootstrap.py: |
{{ .Files.Get "files/bootstrap.py" | indent 4 }}
